
query User($id: ID!){
  user(id: $id) {
    id
    names
    lastNames
    email
    recipes {
      title
      description
      ingredients
      process
      timeCategory
      nutritionCategory
      portions
      authorId
    }
  }
}